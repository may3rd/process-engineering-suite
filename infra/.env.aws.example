# AWS ECS/Fargate Deployment Environment Configuration
# This file should be used as a reference for configuring AWS Secrets Manager

# Environment Detection
DEPLOYMENT_ENV=aws

# Database Configuration (AWS RDS PostgreSQL)
# Store in AWS Secrets Manager: process-engineering/database-url
# Format: postgresql+asyncpg://username:password@rds-endpoint:5432/engsuite
DATABASE_URL=<from-secrets-manager>

# API Configuration
# Store in AWS Secrets Manager: process-engineering/secret-key
# Generate with: openssl rand -hex 32
SECRET_KEY=<from-secrets-manager>
DEBUG=false

# CORS Configuration
# Store in AWS Secrets Manager: process-engineering/allowed-origins
# Comma-separated list of allowed origins
# Example: https://your-alb-domain.com,https://yourdomain.com
ALLOWED_ORIGINS=<from-secrets-manager>

# Frontend Configuration
# API URL for ECS service-to-service communication
# Uses ECS service discovery - 'api' is the ECS service name
NEXT_PUBLIC_API_URL=http://api:8000

# AWS Configuration
AWS_REGION=us-east-1
AWS_SECRETS_ARN=arn:aws:secretsmanager:us-east-1:ACCOUNT_ID:secret:process-engineering/*

# Infrastructure Details (for reference)
# RDS Instance: db.t3.medium, Multi-AZ
# ECS Cluster: process-engineering-cluster
# VPC: Private subnets for ECS/RDS, public subnets for ALB
# ALB: Application Load Balancer with HTTPS
# ECR Repositories:
#   - process-engineering/api
#   - process-engineering/web
#   - process-engineering/network-editor
#   - process-engineering/psv
#   - process-engineering/design-agents
