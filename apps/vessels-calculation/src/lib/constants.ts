/**
 * Vessel calculator constants.
 * All values are in SI base units (mm, m, kg, etc.).
 */

/** Steel density for shell/head mass estimation (kg/m³) */
export const STEEL_DENSITY_KG_M3 = 7850

/** Minimum head depth fraction of diameter for conical heads (no minimum) */
export const MIN_CONICAL_DEPTH_FRACTION = 0.1

/** Maximum wetted area height cap per API 2000 (mm) */
export const WETTED_AREA_HEIGHT_CAP_MM = 9144
