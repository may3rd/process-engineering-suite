# PSV Application Environment Variables

# API URL (required for production mode)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Use localStorage for demo mode (no API required)
# Set to 'true' to enable offline/demo mode
# Set to 'false' when using the Python API backend
NEXT_PUBLIC_USE_LOCAL_STORAGE=true
