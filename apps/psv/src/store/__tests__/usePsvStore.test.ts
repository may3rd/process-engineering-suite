import { describe, it, expect } from "vitest";
import { usePsvStore } from "../usePsvStore";

describe("usePsvStore", () => {
  it("should be defined", () => {
    expect(usePsvStore).toBeDefined();
  });
});
